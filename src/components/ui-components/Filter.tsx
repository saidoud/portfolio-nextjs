import React from 'react';
import { Stack, Chip } from '@mui/material';

function Filter({ selected, data }) {
    return <Stack direction="row" justifyContent={'center'} alignItems="center" spacing={4} sx={{ my: 4 }}></Stack>;
}

export default Filter;
